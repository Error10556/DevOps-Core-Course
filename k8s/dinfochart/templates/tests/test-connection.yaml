apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dinfochart.fullname" . }}-test-connection"
  labels:
    {{- include "dinfochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dinfochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
