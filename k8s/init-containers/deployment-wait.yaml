apiVersion: apps/v1
kind: Deployment
metadata:
  name: wait
spec:
  initContainers:
    - name: wait-for-service
      image: busybox:1.36
      command: ['sh', '-c', 'until nslookup myservice; do sleep 2; done']
